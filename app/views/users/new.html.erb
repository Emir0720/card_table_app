<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">

    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
        <h1 class="text-2xl font-bold mb-4">Sign Up</h1>

        <%= form_with model: @user, url: users_path, local: true, class: "space-y-4" do |f| %>
            <div class="flex flex-col mb-4">
                <%= f.label :name, class: "text-sm font-semibold" %>
                <%= f.text_field :name, class: "border rounded p-2 mt-1 w-full" %>
            </div>
            <div class="flex flex-col mb-4">
                <%= f.label :email, class: "text-sm font-semibold" %>
                <%= f.email_field :email, class: "border rounded p-2 mt-1 w-full" %>
            </div>
            <div class="flex flex-col mb-4">
                <%= f.label :password, class: "text-sm font-semibold" %>
                <%= f.password_field :password, class: "border rounded p-2 mt-1 w-full" %>
            </div>
            <div class="flex flex-col mb-4">
                <%= f.label :password_confirmation, class: "text-sm font-semibold" %>
                <%= f.password_field :password_confirmation, class: "border rounded p-2 mt-1 w-full" %>
            </div>
            <div>
                <%= f.submit "Sign Up", class: "bg-green-500 text-white rounded p-2 hover:bg-green-700 w-full" %>
            </div>
        <% end %>
    </div>

</body>
</html>
