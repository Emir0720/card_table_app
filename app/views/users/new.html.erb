<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #89f7fe 0%, #66a6ff 100%);
        }

        .form-container {
            transition: transform 0.5s ease-in-out, box-shadow 0.5s ease-in-out, background-color 0.5s ease-in-out;
            position: relative;
        }

        .form-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(45deg, rgba(255, 255, 255, 0.3), rgba(255, 255, 255, 0));
            border-radius: 0.75rem; 
            opacity: 0;
            transform: rotate(0deg) scale(0.9);
            transition: transform 0.7s ease-in-out, opacity 0.7s ease-in-out;
            z-index: -1;
        }

        .form-container:hover::before {
            opacity: 1;
            transform: rotate(5deg) scale(1);
        }

        .form-container:hover {
            transform: scale(1.08) rotate(1deg);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
            background-color: #f7fafc;
        }

        .input-field {
            transition: background-color 0.3s ease-in-out, transform 0.3s ease-in-out;
        }

        .input-field:focus {
            background-color: #edf2f7;
            transform: scale(1.02);
        }
    </style>
</head>
<body class="flex items-center justify-center min-h-screen p-4">

    <div class="bg-white p-8 rounded-xl shadow-xl w-full max-w-md space-y-8 form-container">
        <h1 class="text-3xl font-bold text-center mb-6 text-gray-800">Sign Up</h1>

        <%= form_with model: @user, url: users_path, local: true, class: "space-y-6" do |f| %>
            <div class="mb-5">
                <%= f.label :name, class: "block text-sm font-semibold text-gray-700" %>
                <%= f.text_field :name, class: "mt-1 block w-full px-4 py-3 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm input-field", required: true %>
            </div>
            <div class="mb-5">
                <%= f.label :email, class: "block text-sm font-semibold text-gray-700" %>
                <%= f.email_field :email, class: "mt-1 block w-full px-4 py-3 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm input-field", required: true %>
            </div>
            <div class="mb-5">
                <%= f.label :password, class: "block text-sm font-semibold text-gray-700" %>
                <%= f.password_field :password, class: "mt-1 block w-full px-4 py-3 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm input-field", required: true %>
            </div>
            <div class="mb-5">
                <%= f.label :password_confirmation, class: "block text-sm font-semibold text-gray-700" %>
                <%= f.password_field :password_confirmation, class: "mt-1 block w-full px-4 py-3 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm input-field", required: true %>
            </div>
            <div>
                <%= f.submit "Sign Up", class: "w-full bg-green-500 text-white py-3 px-4 rounded-lg shadow-md hover:bg-green-600 focus:outline-none focus:ring-2 focus:ring-green-500 transition duration-300" %>
            </div>
        <% end %>

        <div class="text-center mt-4">
            <%= link_to "Already have an account? Login", login_path, class: "text-sm font-semibold text-indigo-600 hover:text-indigo-800 transition-colors duration-300" %>
        </div>

    </div>

</body>
</html>
