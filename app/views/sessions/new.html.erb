<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">

    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
        <h2 class="text-2xl font-semibold text-center mb-6">Login</h2>
        
        <%= form_with url: login_path, local: true, class: "space-y-4" do |f| %>
            <div class="mb-4">
                <%= f.label :email, class: "block text-sm font-semibold text-gray-700" %>
                <%= f.email_field :email, class: "mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm", required: true %>
            </div>
            <div class="mb-4">
                <%= f.label :password, class: "block text-sm font-semibold text-gray-700" %>
                <%= f.password_field :password, class: "mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm", required: true %>
            </div>
            <div>
                <%= f.submit "Login", class: "w-full bg-blue-500 text-white py-2 px-4 rounded-lg hover:bg-blue-600" %>
            </div>
        <% end %>

        <div class="text-center mt-4">
            <%= link_to "Sign Up", signup_path, class: "text-sm font-semibold text-indigo-600 hover:text-indigo-700" %>
        </div>
    </div>

</body>
</html>
